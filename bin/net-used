#!/usr/bin/bash

stat=$(vnstat -q | head -n 5)

if [[ $stat == *"Not enough data"* ]]; then
	echo ...
	exit 0
elif [[ $stat == *"No database found"* ]]; then
	echo N/A
	exit 0
fi

echo $stat | cut -d ":" -f7 | tr -d " "
